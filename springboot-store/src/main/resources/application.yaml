#server配置
server:
  port: 8080
  servlet:
    context-path: /

#連接池配置
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      url: jdbc:mysql://shop_db
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver

#mybatis-plus配置
mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
